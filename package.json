{
  "name": "streaming-overlay-app",
  "version": "1.0.0",
  "description": "",
  "main": "electron-main.js",
  "scripts": {
    "start": "node server/server.js --ignore client",
    "develop": "cross-env NODE_ENV=development concurrently \"cd server && npm start\" \"cd client && npm run dev\" \"npm run electron\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "seed": "cd server && npm run seed",
    "build": "cd client && npm run build",
    "render-build": "npm install && npm run build",
    "electron": "electron ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0",
    "cross-env": "^7.0.3",
    "electron": "^34.2.0",
    "vite": "^6.1.1"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "electron-store": "^10.0.1",
    "dotenv": "^16.4.7"
  }
}
